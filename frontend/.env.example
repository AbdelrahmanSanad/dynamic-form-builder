# Base URL for the API. In dev, leave as /api and Vite proxies to the backend.
# In production behind a single origin, /api is also correct.
VITE_API_BASE_URL=/api

# Optional: override the dev proxy target (defaults to http://localhost:4000).
VITE_API_PROXY=http://localhost:4000
